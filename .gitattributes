# Written wholesale by `warden sync` and never edited in place, so a corpus change would otherwise
# flood review with a diff nobody reads. They stay committed rather than gitignored: the source is
# `warden/claude/`, and a reviewer reads the change there.
.claude/agents/**        linguist-generated=true -diff
.claude/commands/**      linguist-generated=true -diff
