/** Resumable-scope name the Tabs root stamps and the client scope registers. @public */
export const TABS_SCOPE = "tabs";

/** A tab, by role. @public */
export const TAB_SELECTOR = "[role='tab']";

/** The tablist a {@link TAB_SELECTOR} match belongs to. @public */
export const TABLIST_SELECTOR = "[role='tablist']";
