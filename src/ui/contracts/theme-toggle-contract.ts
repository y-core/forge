/** Resumable-scope name `ThemeToggle` stamps and the chrome client scope registers. Eager. @public */
export const THEME_SCOPE = "theme";

/** The action a theme toggle names in `data-on-click`. @public */
export type ThemeAction = "cycleTheme";
