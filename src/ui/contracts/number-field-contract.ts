/**
 * Shared NumberField wiring — imported by BOTH the SSR component (`ui/core`) and the client scope
 * (`ui/core/client.ts`). Pure data, side-effect-free.
 */

/**
 * Resumable-scope name the NumberField root stamps and the client scope registers.
 * @public
 */
export const NUMBER_FIELD_SCOPE = "number-field";
