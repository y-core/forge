// The row shape is stated inline: it has one reader, and a `types.ts` beside a fixture would be a
// second module only a test reaches.
/** The components `catalog.md` still owes a "Job → component" row, each with the task that owes it. The list only shrinks. @internal */
export const CATALOG_MISSING: readonly { key: string; owner: string }[] = [];
