import * as v from "valibot";

/** Result of a validation pass: success carries the parsed data, failure carries human-readable error messages. @public */
export type { ValidationResult } from "../result/result";
/** The valibot namespace — forge's canonical validation entry point. @public */
export { v };
