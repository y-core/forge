.DS_Store

.codex
.reference
.zed/

.dev.vars
.wrangler/

bin/
/coverage/
node_modules/
tmp/

# Generated assets module — a consumer-side build artifact, never committed.
.forge/

# Playwright run artifacts — `bun run release` refuses a dirty tree, so these must never be tracked.
playwright-report/
test-results/

*.test
*.out
