.DS_Store

.codex
.reference
.zed/

.dev.vars
.wrangler/

bin/
coverage/
docker/
node_modules/
tmp/

# Playwright run artifacts — `bun run release` refuses a dirty tree, so these must never be tracked.
playwright-report/
test-results/

*.test
*.out
