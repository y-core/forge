{
  "seed": "INSERT INTO users (id, email) VALUES (1, 'a@x');",
  "expectSql": ["ALTER TABLE \"users\" ADD COLUMN role TEXT NOT NULL DEFAULT 'member';"],
  "query": "SELECT id, role FROM users ORDER BY id",
  "rows": [{ "id": 1, "role": "member" }]
}
