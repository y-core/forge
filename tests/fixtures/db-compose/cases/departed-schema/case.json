{
  "lib": "CREATE TABLE posts (id INTEGER PRIMARY KEY, title TEXT) STRICT;\n",
  "seed": "INSERT INTO posts (id, title) VALUES (1, 'gone');",
  "refusedWithout": "--allow-destructive",
  "args": ["--allow-destructive", "<digest>"],
  "expectSql": ["DROP TABLE IF EXISTS \"posts\";"],
  "expectStdout": ["posts was declared by lib.sql, which config/db.ts no longer declares or whose file is absent — nothing declares it now"],
  "query": "SELECT name FROM sqlite_master WHERE type = 'table' AND name IN ('users', 'posts') ORDER BY name",
  "rows": [{ "name": "users" }]
}
