{
  "seed": "INSERT INTO parent (id, name) VALUES (1, 'a'), (2, 'b');",
  "custom": "CREATE TABLE child (id INTEGER PRIMARY KEY, parent_id INTEGER NOT NULL REFERENCES parent (id) ON DELETE CASCADE, note TEXT) STRICT;\n",
  "composeRefused": "table child is in the migrations and in no declared schema, and rebuilding parent would drop it",
  "query": "SELECT (SELECT count(*) FROM parent) AS parent, (SELECT count(*) FROM child) AS child",
  "rows": [{ "parent": 2, "child": 0 }]
}
