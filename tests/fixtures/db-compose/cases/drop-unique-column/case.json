{
  "seed": "INSERT INTO t (id, code, note) VALUES (1, 'a', 'x'), (2, 'b', 'y');",
  "refusedWithout": "t: drops column code",
  "args": ["--allow-destructive", "<digest>"],
  "expectSql": ["CREATE TABLE \"_forge_new_t\"", "RENAME TO \"t\""],
  "expectStdout": ["rebuild table t (drop-column-unsupported)"],
  "query": "SELECT id, note FROM t ORDER BY id",
  "rows": [
    { "id": 1, "note": "x" },
    { "id": 2, "note": "y" }
  ]
}
