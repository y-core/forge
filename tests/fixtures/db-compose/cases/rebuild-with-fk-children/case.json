{
  "seed": "INSERT INTO users (id, email) VALUES (1, 'a@x'), (2, 'b@x'); INSERT INTO posts (id, user_id, body) VALUES (10, 1, 'p'), (20, 2, 'q'); INSERT INTO comments (id, post_id, body) VALUES (100, 10, 'c'), (200, 20, 'd');",
  "expectSql": [
    "CREATE TABLE \"_forge_new_users\"",
    "CREATE TABLE \"_forge_new_posts\"",
    "REFERENCES \"_forge_new_users\" (id)",
    "CREATE TABLE \"_forge_new_comments\"",
    "DROP TABLE \"comments\";",
    "DROP TABLE \"posts\";",
    "DROP TABLE \"users\";"
  ],
  "expectStdout": [
    "warning: users.email gains CHECK(LENGTH(EMAIL) < 100) — every existing value must already satisfy it; rehearse it on `standby` first"
  ],
  "query": "SELECT (SELECT count(*) FROM users) AS users, (SELECT count(*) FROM posts) AS posts, (SELECT count(*) FROM comments) AS comments, (SELECT sql FROM sqlite_master WHERE name = 'comments') AS comments_sql",
  "rows": [
    {
      "users": 2,
      "posts": 2,
      "comments": 2,
      "comments_sql": "CREATE TABLE \"comments\" (id INTEGER PRIMARY KEY, post_id INTEGER NOT NULL REFERENCES \"posts\" (id) ON DELETE CASCADE, body TEXT) STRICT"
    }
  ]
}
